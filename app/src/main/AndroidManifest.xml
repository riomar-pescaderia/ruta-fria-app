<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Para que TrackingService pueda volver a prenderse solo después de
         que se reinicia el celular (ver BootReceiver.kt). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.RutaFriaApp"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Recibe el "localizar ahora" que manda Ruta Fría por Firebase
             Cloud Messaging, incluso con la app cerrada/minimizada
             (mientras el sistema no la haya forzado a detenerse del
             todo). Ver FcmService.kt. -->
        <service
            android:name=".FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Servicio en primer plano de tipo "location": mientras está
             corriendo (unos segundos, con una notificación visible que
             dice "Enviando tu ubicación a Ruta Fría...") puede pedir la
             posición del GPS sin necesitar el permiso de ubicación "todo
             el tiempo" — alcanza con el permiso normal de ubicación. -->
        <service
            android:name=".LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Seguimiento automático en horario laboral (ver
             TrackingService.kt) — queda corriendo en primer plano con su
             propia notificación mientras dure la sesión. -->
        <service
            android:name=".TrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Vuelve a prender el seguimiento después de un reinicio del
             celular, si correspondía que estuviera corriendo. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
